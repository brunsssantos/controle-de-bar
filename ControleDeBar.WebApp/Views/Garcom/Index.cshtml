@{
    ViewData["Title"] = "Visualização de Garçons";
}

@model VisualizarGarconsViewModel;

<div class="container">
    <div class="d-flex justify-content-end mb-3">
        <a class="btn btn-primary" asp-action="Cadastrar">+ Novo Garçom</a>
    </div>
</div>


<div class="row g-3">

    @foreach (DetalhesGarcomViewModel g in Model.Registros)
    {
        <div class="col-md-4 col-lg-3">

            <div class="card h-100 bg-dark border-secondary">
                <div class="card-body">
                    <h5 class="card-title">@g.Nome</h5>
                    <p class="card-text mb-1"><strong>CPF:</strong> @g.Cpf</p>
                </div>

                <div class="card-footer text-end border-secondary">
                    <a class="btn btn-primary me-1" href="/Garcom/Editar/@g.Id">
                        <i class="bi bi-pencil-square "></i>
                    </a>
                    <a class="btn btn-danger" href="/Garcom/Excluir/@g.Id">
                        <i class="bi bi-trash3"></i>
                    </a>
                </div>
            </div>
        </div>
    }
</div>